# Phoenix Service Makefile
# This is a stub to document the service - all targets are in build.mk

.DEFAULT_GOAL := help

.PHONY: help
help:
	@echo "Phoenix service targets (run from repository root):"
	@echo "  make phoenix-install    - Install Phoenix to cluster"
	@echo "  make phoenix-uninstall  - Uninstall Phoenix from cluster"
	@echo "  make phoenix-dashboard  - Start dashboard with port-forward"
