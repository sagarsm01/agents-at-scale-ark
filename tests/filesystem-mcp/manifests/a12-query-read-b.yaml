apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: filesystem-read-b
  annotations:
    "ark.mckinsey.com/mcp-server-settings": '{"($namespace)/mcp-filesystem": {"toolCalls": [{"name": "set_base_directory", "arguments": {"path": "workspace-b"}}]}}'
spec:
  input: |
    Read the file "agent-b-data.txt" and confirm it contains Agent B's data.

    Then list all files in the current directory.
  targets:
  - type: agent
    name: filesystem-agent-b
